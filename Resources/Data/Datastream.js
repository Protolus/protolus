this.Datastream = new Class({
    Implements: Options,
    initialize : function(options){
        this.setOptions(options);
    },
    fireEvent : function(options, callback){
        
    },
    addEvent: function(type, callback){
        return value;
    }
});

