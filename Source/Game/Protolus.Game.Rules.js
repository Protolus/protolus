Protolus.Game.Rules = new Class({
    Extends : Protolus.Game.Phase,
    initialize : function(mode, maxPlayers, minPlayers, gui){
        this.gui = gui;
    },
    play : function(players, board){
        
    }
});